[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npm run build && npm run db:generate"

[deploy]
startCommand = "cd backend && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.environment]
NIXPACKS_NODE_VERSION = "18"